# Judging alignment

Maps each rubric criterion (from `COMPETITION.md`) to the concrete artifact in this repo that
satisfies it, and its status. This is what stops a team building something judges never actually
score. **A criterion with no row, or a row with no artifact, is a gap to close before submission.**

Rubric is not published yet (`COMPETITION.md` rubric table is `TBD`), so this is a skeleton.
`TODO(Mulaydm10)`: populate the Criterion column verbatim from the real rubric the moment it's
published, then fill Artifact + Status for each row before the deadline, not after.

| Criterion (from COMPETITION.md) | Artifact in this repo | Status |
|---|---|---|
| `TBD` — rubric not published yet | `TBD` | Gap |
