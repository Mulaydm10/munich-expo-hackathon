> **LOCKED governing file.** Do not edit in place. See `GOVERNANCE.md`.

# VISION.md — the thesis

**Not filled in yet, deliberately.** No idea, target user, or "what working looks like" has been
given for this project as of scaffold time. Every slot below is `TBD` rather than a guess — a
plausible-sounding fabricated thesis is far more expensive to catch and unwind later than an obvious
blank, especially once other files start linking to it. This is registered as `Q-0001` in
`research/open_questions.md`.

`TODO(Mulaydm10)`: fill in every section below once the idea is chosen, ideally before or right at
kickoff — most of the rest of the repo (design decisions, demo scenarios, judging alignment) depends
on this being real.

## What we're building
`TBD`

## For whom
`TBD` — who is the user/beneficiary, specifically (not "developers" or "everyone")

## Why it isn't already solved
`TBD` — what exists today, and what's actually missing or broken about it (this is the thing judges
and prior-art checks will probe hardest; do not skip it)

## What "working" looks like at demo time
`TBD` — the concrete, observable bar for a successful demo. This should be specific enough to
directly seed `DEMO.md`'s first scenario once there is a build.

## Explicit non-goals
`TBD` — what this project deliberately will NOT attempt, to keep scope inside the deadline
