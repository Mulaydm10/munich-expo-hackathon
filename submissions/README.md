# submissions/ — staging for the actual deliverable(s)

Not locked — plain file, edit freely. Stage the actual submission artifacts named in
`COMPETITION.md`'s "Submission format" section here (currently `TBD`).

`TODO(Mulaydm10)`: once `COMPETITION.md`'s submission format is filled in, replace this paragraph
with the real expected artifact(s) (e.g. "a zip of X", "a link to Y", "slides at Z") and keep this
directory's contents in sync with what's actually going to be submitted.

Nothing staged yet — there is no build to submit.
